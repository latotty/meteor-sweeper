import { ControlLabel } from 'react-bootstrap';
import styled from 'styled-components';

export const BlockControlLabel = styled(ControlLabel)`
  display: block;
`;

export default BlockControlLabel;
